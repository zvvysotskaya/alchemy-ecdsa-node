{
  "version": 2,
  "builds": [
    {
      "src": "server/index.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      // Specify which paths will route to a destination using a regex
      "src": "/(.*)",
      
      "dest": "server/index.js"
    }
  ]
}